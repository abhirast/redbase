/*
 * sm_test.2: duplicate relation names
*/

/* duplicate relation names */
create table soaps(soapid  i, sname  c28, network  c4, rating  f);
create table soaps(starid  i, stname  c20, plays  c12, soapid  i);

exit;